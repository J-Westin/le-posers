keras_test_1a.py

Code based on the keras_example.py from https://gitlab.com/EuropeanSpaceAgency/speed-utils

It uses VGG16 pretrained CNN as an example and adds 4 extra fully conected layers, then trains.

Important to set variable speed_root to driectory where images are saved
